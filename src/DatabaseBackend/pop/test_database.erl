%%This file is to populate the database


 -module ('test_database').
%% %% -include ("air_database.hrl").

%% %% -export ([populate/0]).

%% populate() ->
%%     {ok, Pid} = amnesia:open(air_database),

    %% adding flights
    %% {ok, Flight1} = amnesia:add_new (Pid, #flightno {flight_no = "SKS101", flight_to = "STOCKHOLM" ,flight_from = "UPPSALA", flight_take_off = 1337 , flight_arrival_time = 1437}),
    %% %% adding passangers
    %% {ok, Pass1} = amnesia:add_new (Pid, #passengerno {passenger_no = 00001, name = "Zkalman", address = "birdie, Rad 4, plats7", payment_info = 2112121212, email = "Galenskap@birdie.org"}),
    %% ok.
    %% adding seats
%% {ok, S1} = amnesia:add_new (Pid, #seats{})  

%%     %% ... and these are the order lines
%%     amnesia:add_new (Pid, #order_line { orders = Order, product = P2, quantity = 3 }),
%%     amnesia:add_new (Pid, #order_line { orders = Order, product = P1, quantity = 10 }),

%%     %% finally, let's add a new product and another order using a single function call
%%     amnesia:add_new (Pid, [ #product { product_code = "004", description = "Data Server", price = 5200.00 },
%% 			    #orders { order_number = 31, order_date = {2008, 7, 20}, customer = Cust1},
%% 			    #order_line { orders = '$2', product = P3, quantity = 2},
%% 			    #order_line { orders = '$2', product = '$1', quantity = 11 } ]),
%%     ok.
%%DETTA KOMMER ATT FINAS SNART KOMMER ATT LADDA UPP DB BLAD MM SNART
